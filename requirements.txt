# HD-Python模块依赖
#
# 基本依赖（根据实际使用情况调整）
# 注意：此模块主要使用标准库，但为了方便开发和发布，列出可能需要的依赖

# 开发依赖
setuptools>=60.0.0
wheel>=0.38.0
build>=0.7.0

# 测试依赖（可选）
pytest>=7.0.0
pytest-cov>=4.0.0

# 如果项目使用了numpy等第三方库，取消下面的注释
# numpy>=1.19.5

# 文档构建依赖（可选）
# sphinx>=5.0.0
# sphinx-rtd-theme>=1.0.0